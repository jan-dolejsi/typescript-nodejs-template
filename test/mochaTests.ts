const expect = require('chai').expect;
// import { PlanView } from './PlanView';

describe("MyClass", () => {
    describe("#method()", () => {
        it("does something", () => {
            // new PlanView(host, );
            expect(true).to.equal(true);
        });
    });
});